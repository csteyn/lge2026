# Model log

A running engineering record: decisions with their reasons, errors found (including ones caught before anything was published), and obstacles. Newest first. Numbers in brackets refer to checks (C..) and assumptions (A..) on the site.

## Open obstacles

- **O10. The ANC is under-predicted against both the poll and 2024, and the interpolated basis did not change that (L046).** The ANC's forecast share barely moved when the premium basis changed. A step-by-step decomposition from the 2024 result to the forecast (L046) will show which ingredient pulls it down; candidate mechanisms are listed there before the result. The poll gap is not a target.
- **O9. Resolved (L046).** Newcomer model v2 passed its pre-registered test at the adopted settings and is in use.
- **O7. Largely resolved (L040).** On ward Brier score the model now beats the national-vote rule (0.157 against about 0.182); on plain accuracy it trails by about 3 of 406 wards (90.1% against 90.9%).
- **O8. Closed (L041).** Polls are not used as inputs; their Western Cape record in 2024 shows a consistent under-estimate of the DA that one data point per pollster cannot correct. They are published as a cross-check (Polls page, check C19).
- **O1. The live data path is partly verified** (IEC local-election reports and MDB 2026 layers fetched and parsed on 2026-09-24; national-election files, candidate lists and the full live run not yet). Each remaining step is treated as untested until it has run on real files.
- **O2. Historic VD boundaries are not published** (IEC delimits VDs with its own GIS). The VD concordance therefore keys on VD number; check C04 reports how many votes fall in VDs that no longer exist.
- **O3. 2026 council sizes need the MEC section 12 notices.** Until entered in `data-raw/manual/council_seats_2026.csv`, sizes are derived and check C07 fails.
- **O4. Resolved (L018).**
- **O6. Resolved (L021).**
- **O5. Resolved for the Patriotic Alliance (L023).** The National Coloured Congress, the People's Movement for Change and the ATM have too little by-election evidence and keep the prior; their intervals are correspondingly wide.

## 2026-09-26

**L046. Newcomer v2 adopted at the new settings; the premium change is within noise; a decomposition to find what holds the ANC down.**

*The rerun required by L044's order.* At `premium_basis: interpolated`, the 2021 backtest with 2016-fitted coefficients gives seat CRPS 0.3996 without newcomers and 0.3790 with (paired difference -0.021, 90% bootstrap interval -0.034 to -0.009) and 90% coverage 87.9%. Condition (1) holds. C20 passes with the module in use. **v2 is adopted** (`entrants: true`, commit `0d3ec1b`, made by the maintainer by applying the rule mechanically, before this entry was written). Outside the rule: council control improves from 19 to 21 councils right (Brier 0.354 to 0.244), recovering what the premium change cost, and seats won by parties the model could not see fall from 46 to 0.

*The premium decision, seen with its interval.* The bootstrap interval added in L045 shows the old basis within noise of the new one: seat CRPS +0.0036 relative to the adopted setting, 90% interval -0.009 to +0.018. The data do not distinguish the two bases. The decision stands, because the rule chose it and no rule was set for a tie, but it should be described as "no worse, and right on the DA's level in 2021", not as a demonstrated improvement. Full-strength premiums, by contrast, are clearly worse (+0.016, interval +0.004 to +0.027).

*What changed in 2026.* The DA's forecast share fell, mostly from the newcomers now being in the model. The ANC's barely moved: the interpolated basis did not close its gap with the poll or with 2024 (O10). The PA's by-election premium rose from 0.17 to 0.41 and the prior for the other parties without a fitted translation from -0.15 to +0.05. This is expected, not a new finding about the PA: premiums are measured relative to the fitted parties' translation, and interpolation raised the fitted parties' premiums by a roughly common amount (about 0.2 after halving). The PA's forecast share relative to the DA's barely moved. This is reasoning from the outputs, not a separate test.

*A diagnostic before any further change (O10).* Why is the ANC's forecast so far below its 2024 result, and the DA's too? The new output `share_decomposition.csv` starts from the 2024 result and adds the model's ingredients one at a time, for the province and each council: (0) the 2024 result in the model's party groups; (1) the same, on the 2026 voting districts and registration, as a closure check; (2) with each district's turnout from the 2021 local election; (3) after the national-to-local translation; (4) plus the premiums of parties without a fitted translation; (5) plus newcomers at their median shares; (6) the simulation median. Candidate mechanisms, written down before the result: (a) *turnout*: the 2021 local election was held under Covid conditions with low and uneven turnout, and carrying its district pattern into 2026 would lower the share of parties whose voters stayed home most (step 2); (b) *translation*: a negative learned premium carried from 2021 (step 3); (c) *parties without a fitted translation*, chiefly the PA (step 4); (d) *newcomers* (step 5); (e) *convexity*: symmetric random variation on the log scale pulls large parties' shares down, and the ward and district spreads are large (step 6; a toy calculation with spreads of 0.3-0.7 gives 1-5 points off a 55% party). **Commitment:** the decomposition locates mechanisms and changes nothing. Any change it suggests is made only through a rule fixed before a backtest result, and the backtest cannot test (a), because 2016 was not a Covid election; that limit will be stated with any proposal.

**L045. Results of the L044 experiments: the interpolated premium basis is adopted; newcomer v2 passes at the old settings and is re-tested at the new ones.** The rules were committed and pushed before the run (commit `baa3bcf`, dated 26 September 2026 08:50 SAST; a commit date comes from the committer's clock, and GitHub's push record is the server-side evidence). The same commit also added `first_timers.csv` at the project root by mistake: historical newcomer data exported for the redesign, not a forecast output. It is removed.

*Premium basis.* The 2021 backtest, 400 draws per variant with the same random numbers, 390 common cases:

| Variant | Seat CRPS | 90% coverage | Ward Brier | Wards correct | Control Brier | Councils correct | DA 2021 | ANC 2021 |
|---|---|---|---|---|---|---|---|---|
| earlier, halved (current) | 0.4050 | 88.5% | 0.1571 | 89.9% | 0.298 | 21 of 25 | 53.4% | 24.3% |
| earlier, full | 0.4175 | 85.8% | 0.1751 | 87.7% | 0.363 | 18 | 54.9% | 21.1% |
| **interpolated, halved** | **0.4014** | 88.0% | 0.1513 | 90.4% | 0.339 | 19 | 54.7% | 24.9% |
| interpolated, full | 0.4083 | 86.6% | 0.1634 | 87.9% | 0.449 | 14 | 57.1% | 22.4% |

Actual 2021: DA 54.7%, ANC 20.5%. Both full-strength variants fail the ward condition (0.018 and 0.006 worse than current). The interpolated basis at half strength is eligible, best, and lower than the current model on seat CRPS by 0.0036 (0.9%). **Under the rule it is adopted: `premium_basis: interpolated`** (A20). What the result shows, and what it does not:

- It fixed the mechanism it was designed for in the DA's case: the predicted DA share moved from 53.4% to 54.7%, against 54.7% actual.
- It did not help the ANC in 2021: 24.3% became 24.9%, against 20.5%. The ANC's 2021 fall was larger than either basis learned from 2014-2016. The backtest therefore supports the change on overall scores, not as a fix for the ANC. Its effect on the 2026 ANC gap (O10) will be seen in the next run; that gap played no part in the decision and will not.
- The margin is small. The comparison is paired (shared random numbers), but the rule required no interval. From the next run the grid also reports each variant's paired difference from the current model with a 90% bootstrap interval over councils, as the newcomer test does. This is reporting only; it cannot reverse the decision.
- A cost outside the rule: council control got worse (Brier 0.298 to 0.339; the most likely outcome right in 19 councils rather than 21). Control is scored in 2026 (SCORING.md) but was in neither the ward rule (L039) nor this one. Two councils of 25 is within noise, but it is reported, not explained away. **Lesson:** future experiments include council control as a guard condition. It is not applied retroactively, because changing a rule after seeing its result defeats the purpose of fixing it first.

*Newcomer v2, at the old settings.* Coefficients fitted on 2016's newcomers only, 147 newcomer cases in 2021. Seat CRPS 0.4031 without newcomers, 0.3854 with (paired difference -0.018, 90% bootstrap interval -0.031 to -0.007), and 90% coverage 87.7%: condition (1) holds. Check C20 for 2026: no council's median newcomer total exceeds the historical 95th percentile (21.1%): condition (2) holds. C21: no coefficient differs by more than two standard errors between the 2016-only and pooled fits; the nearest is ward coverage (1.38 against 2.35, z -1.9). Also improved, outside the rule: council control (Brier 0.308 to 0.212; 22 councils right rather than 20), and seats won by parties the model could not see (46 to 0). Ward scores are unchanged.

*A regime difference the backtest cannot test.* The 2016-only fit finds no party-level correlation (rho 0: too few parties stood in several councils in 2016); the pooled fit used for 2026 has rho 0.33. Rho does not change any single council's distribution, which is all the seat scores measure, but it widens the uncertainty of a wide party's total across councils. This is the L043 lesson in a milder form: recorded, and not a failure of the rule.

**Decision, following the L044 order:** the premium experiment changed the configuration, so the newcomer test is rerun at `premium_basis: interpolated`, with `entrants` still false. If the rerun's `adopt` is TRUE and C20 still passes, the module is switched on; otherwise it stays off.

**L044. Newcomer model v2 and a premium-basis experiment: built, with both rules fixed before any result.** Nothing in this entry was written after a backtest or forecast result from this code had been read.

*Newcomer model v2 (O9).* The past-newcomer data (232 party-council cases, 104 party-years: 82 cases from 2016, 150 from 2021) were examined first. They confirmed the three L043 flaws. The "11+ councils, all wards" class holds 14 cases from only 4 parties, and the PA supplies the large shares (13.9-21.9% in Beaufort West, Laingsburg and Prince Albert). A newcomer standing in every ward of a small council is a different thing from one standing in every ward of Cape Town: Cape Town's newcomers took 0.8% combined in 2016 and 5.8% in 2021. Past councils' newcomer totals have a median of 4.3%, a 90th percentile of 15.8% and a 95th of 21.1%. v2 replaces the classes with one regression. Each past newcomer's council share (log-odds) is regressed on ward coverage, standing in every ward, breadth (log councils stood in), council size (log registered voters) and history elsewhere in the province. Each party-year is **weighted once**, however many councils it stood in, so no single party can dominate. Draws are logit-normal around the prediction. The residual spread is split between a component a party shares across its councils (rho) and a council-specific one. A sourced override (the SACP, A17) still replaces the prediction for its party. The combined newcomer share per council is capped at 80%. On all 232 cases the fit gives coverage +2.4, breadth -0.43, council size about -1.0 per log unit, history about 0; weighted R² 0.74, residual sd 1.1, rho 0.33. A preview on the 2026 lists brings Cape Town's newcomers from about a third of the vote (v1) to a few percent, and puts no council above the historical 95th percentile. (2026 figures stay private until the publication decision, L029.) The preview used council seats as the size measure. The built model uses registered voters, which the results themselves give for every council in both years, while 2026 seat counts depend on the section 12 notices (O3). *A caveat on blindness:* the model's form was chosen after looking at all past newcomers, including 2021's, which are part of the 2021 backtest's truth. The backtest fits the coefficients on 2016's newcomers only, but the choice of predictors was not blind to 2021. This favours v2 in the backtest by an unknown amount, which is one reason the rule below adds a test that does not depend on 2021.

**Rule for v2, fixed now:** adopt it only if **both** hold. (1) In the 2021 backtest, with the coefficients fitted on 2016's newcomers only, seat CRPS is lower with newcomers than without, and 90% PIT coverage (modelled parties) lies in [0.80, 0.97]: the L035 rule, unchanged. (2) Check C20 passes for 2026: no council's median simulated newcomer total exceeds the 95th percentile of past councils' totals. To make (2) testable while the module is off, newcomers are now always drawn as a diagnostic; the forecast uses them only when `model.entrants` is true, and the assumptions page says which. New check **C21** reports the L043 regime lesson: coefficients fitted on 2016 alone against 2016 and 2021 pooled, flagging any that differ by more than two combined standard errors. C21 is reported, not part of the rule.

*Premium basis (O10).* A party's learned local premium `a` compares its 2021 local vote with its 2019 national vote. Anything that moved between May 2019 and November 2021 is learned as "local", and carried into 2026 as if it were a fixed feature of local elections. The DA's 53-seat overshoot in the 2021 backtest (L027) fits this mechanism, and so does the 2026 ANC gap. The alternative measures the premium against the national vote **interpolated to the local election's date**, on the log-ratio scale, between the national elections either side: `(1 - f) clr(NPE_before) + f clr(NPE_after)`, with `f` the fraction of the interval elapsed (0.448 for 2016, from 2014 and 2019; 0.491 for 2021, from 2019 and 2024). The later national election is known in both cases: 2019 when the backtest predicts 2021, 2024 when the forecast predicts 2026. VDs present in only one of the two national elections are dropped from the fit.

**Rule for the premium basis, fixed now:** four variants, basis {earlier (current), interpolated} x premium strength {halved (current), full}, every other setting as in `config.yml`, 400 draws each. Choose the lowest seat CRPS among variants whose 90% coverage is in [0.80, 0.97] and whose ward Brier score is no more than 0.005 worse than the current model's; adopt it only if it beats the current model on seat CRPS. If none qualifies, nothing changes. The ward condition protects the L040 gains; it was added when the code was written, before any result, and makes the rule as first described to the maintainer stricter. Each variant also reports the DA's and ANC's predicted and actual 2021 provincial share, as explanation only; the ANC's 2026 poll gap plays no part in the decision.

**Order, fixed now:** the premium experiment is decided first. If it changes `config.yml`, the newcomer test is rerun at the new settings, and v2 is adopted only if it passes there.

*Errors caught before any use.* A helper named `pick()` inside `mutate()` was silently replaced by dplyr's own `pick()`, so the new experiment failed on its first test run; it is renamed. The backtest-contract test did not know the new interpolation input; it now checks it. No result was produced by either.

## 2026-09-25

**L043. The poll cross-check exposed an implausible 2026 forecast: the newcomer module is switched off pending redesign.** With the module on, the model's Western Cape PR vote was DA 34.4% (90% range 26-43) and ANC 10.5%, against Ipsos's 47% and 25% and the 2024 provincial result of 55.3% and 19.6%; in Cape Town, DA 30.2% and ANC 8.6% against 46% and 26%. The gaps exceed any past poll error. *Cause:* newcomers took about 23.5% of the provincial PR vote (sum of 55 parties' medians) and about 32% of Cape Town (21 newcomers), and every newcomer's share is taken proportionally from everyone else. *Why the priors were so large:* (1) the "11+ councils, all wards" class is effectively one party, the PA's 2021 surge (30 cases from 3 parties in 2026 draw a median of 4.7% per council, 95th percentile 17%), which L041 flagged for the SACP but did not fix generally; (2) the "1 council, all wards" class (median 4.6%) ignores council size: a civic contesting every ward of a small council is not comparable with a new party contesting all 116 wards of Cape Town; (3) nothing checked the newcomers' combined share against history. *Process failure:* the backtest that adopted the module (L038, L040) used a prior from 2016's newcomers only, while the 2026 forecast pools 2016 and 2021, including the PA surge. The configuration in use was never tested in the regime where it is applied, so the adoption rule could not catch this. **Decision:** the module is switched off (`entrants: false`), returning to the configuration already tested without it. This deviates from the L038 adoption, on evidence independent of any 2026 result: the 2024 results, the polls and history. New check C20, critical, compares each council's simulated newcomer total with the 95th percentile of past councils' newcomer totals; it runs whenever the module is on. The redesign (each party counted once, a minimum number of distinct parties per class, council size as a predictor) will be built against the real data on past newcomers and re-tested with the same pre-registered rule before any use.

**L042. Error in L041, found by following up the SRF's polling: Western Cape poll errors do not all run one way.** L041 stated that every Western Cape poll found had under-estimated the DA in 2024. An SRF Western Cape survey from July 2023 (2 590 registered voters, stated margin +/-2.9%) had the DA *over* the result on the provincial ballot: 64% with 66% turnout modelled (60.4% raw), against 55.30%. It had the ANC at 13% (11.6% raw) against 19.55%, the EFF at 15% (17.6% raw) against 5.30%, and the PA within its margin of error against 7.80%. The track record is therefore: DA errors from -11 (Ipsos, late 2023) to +9 (SRF, July 2023), and -2 (SRF, March 2024); EFF up to +10. The errors are large and variable, not a consistent bias. **The decision in L041 stands on stronger grounds** (there is nothing consistent to correct), but its stated reason was wrong, and the Polls page, the methodology, A19 and the track-record file are corrected. The site had not been published, so no correction notice applies. The lesson is the one L033 taught about files: check the full record before generalising from the part found first. *The SRF's 2026 polling*, with The Common Sense: national (July 2026: ANC 34%, DA 27%, MK 15%, EFF 7%), Johannesburg (DA 42%, ANC 18%), Tshwane (DA about 42-43%, ANC 32%) and eThekwini. Nothing was found for Cape Town or the Western Cape, and no SACP figure. Ipsos remains the only published 2026 Western Cape poll. The "DA will retain Cape Town" report in The Common Sense is an analyst's statistical model, not a poll.

**L041. Current polls: a published cross-check, not an input; a sourced prior for the SACP.**

*What the polls say.* The latest published poll with Western Cape detail is Ipsos Khayabus (face-to-face, 3 600 adults, fieldwork June-July 2026, published 4 September). Among registered voters who chose a party, the Western Cape figures are DA 47%, ANC 25%, PA 9%, EFF 8%, ActionSA 1%, other parties 7%, don't know 3% (stated maximum margin of error +/-5.15). In Cape Town: DA 46%, ANC 26%, EFF 11%, PA 8% (+/-6.12). 84% of Western Cape registered voters said they would vote, the highest of any province. The SRF's latest national figures (DA 27%, ANC 34%) are not broken down for the province in anything found.

*Track record (O8).* Against the 2024 provincial result (DA 55.30%), the three published Western Cape polls found all under-estimated the DA, in the same direction: Ipsos (late 2023) 44%, SRF (March 2024) 53%, and a commissioned survey (late 2023) 42%, which is not strictly comparable because its 18% undeclared stayed in the base. With at most one comparable point per pollster, no bias correction can be estimated, and the 2026 Ipsos figure may carry the same bias. **Decision: polls are not used to narrow the province spread.** Doing so would swap one assumption (a blind spread tested in the backtest) for a larger, untested one (the pollsters' Western Cape bias). They are published instead as a cross-check: a Polls page sets the model's own provincial PR vote (summed across all 25 councils in each simulated election) and its Cape Town vote against the polls and the 2024 track record, and check C19 lists gaps beyond a poll's stated margin, noting that past errors were larger than those margins. O8 closed.

*SACP (A17).* Ipsos reports only parties with 2%+ nationally, and the SACP is not among them, so it is below 2% nationally among registered voters with a preference. Whether it was on Ipsos's electronic ballot is not stated (respondents could add parties). The class prior may not suit it: the Western Cape newcomers that stood in 11+ councils are dominated by the PA's 2021 surge, which would give the SACP a PA-like prior. A sourced override is entered: council share median 0.8%, 90th percentile 2.5%, anchored on the Ipsos bound and scaled by the ANC's weaker Western Cape base (25% against 31% nationally). **The scaling is the maintainers' judgement**, stated as such in the register. The Afrika Mayibuye Movement has no poll evidence and keeps the class prior (C18).

**L040. Decisions from the run carrying L038 and L039.**

*Newcomer confirmation (committed in L038).* At the new spreads (0.35/0.15), seat CRPS with newcomers is 0.4083 against 0.4176 without, so the switch-off condition is not met and **the module stays on**. As before, the seat gain is small and its 90% interval includes zero (-0.038 to +0.022), and seat error rises slightly (0.495 -> 0.535). The clear gain is council control (48% -> 68%; Brier 0.515 -> 0.411).

*Ward experiment (L039).* The rule adopts **slopes fixed at 1, ticket-splitting ratio off, premiums halved**. Ward Brier 0.1846 -> 0.1569; ward winners correct 85.7% -> 90.1%; seat CRPS 0.4082 -> 0.4048 (no cost on seats); coverage 88.1%; council control 68% -> 88%. config.yml follows. **My diagnosis was only partly right.** The errors-in-variables correction, the prime suspect, did not help: the corrected-slope variants were worst on seats (CRPS up to 0.49) and no better on wards. A plausible but untested reason is that dividing by a small reliability inflates small, noisy parties' slopes. Halving the premiums mattered most: it improved ward Brier under every slope choice (fitted 0.1846 -> 0.1607; slopes at 1 0.1767 -> 0.1575). I had discounted this suspect because zeroing the premiums did not help. The evidence now says the premiums carry information but overshoot by roughly half. Slopes at 1 matter in combination: with fitted slopes, halving costs too much on seats (CRPS 0.45, not eligible). The ratio on or off is within noise (0.1569 against 0.1575). Twelve variants chosen on one election invite over-fitting: the pattern is robust (halving helps on wards under every slope choice; slopes at 1 remove its cost on seats), the exact ranking of near-ties is not.

*Why the wards were lost (per-ward table, current model).* Of 406 wards: 340 both right, 30 both wrong, 7 won by the model only, 29 by the national-vote rule only. Of those 29, 24 were ANC wins where the model picked the DA, plus 3 civic wins also called for the DA. This is the DA's learned premium (+0.98 from its strong 2016 local result) carried into 2021, when it declined, the same mechanism as the 53-seat overshoot in L027. They were close calls in the model's own view: it gave the actual winner a median probability of 0.36, and more than 0.3 in 79% of them. The 30 both got wrong are mostly ANC wins both called for the DA (15), plus GOOD, PA and local-party wins.

*O7, reassessed.* A deterministic rule scores a ward Brier of 2 for each wrong pick, so the national-vote rule's 90.9% accuracy is a Brier score of about 0.182, against 0.157 for the adopted variant: on the proper score the model is now better. On plain accuracy it trails by about 3 of 406 wards (90.1% against 90.9%).

*Committed before the next run.* The spreads (0.35/0.15) were chosen with the previous translation settings, and they are not re-tuned now. The next run's backtest reports calibration at the combined settings. If established-party 90% coverage falls outside [0.80, 0.97], the spreads are re-tested with the same grid rule (L027/L037) and logged. The attenuation-correction code stays in the project, unused.

**L039. The ward-winner gap (O7): diagnosis, a correction for attenuated slopes, and a pre-registered experiment.** In the blind 2021 backtest the model picked 85.5% of ward winners correctly, against 90.9% for simply using the 2019 national vote, while beating both simple rules on seats.

*Diagnosis from existing evidence.* It is not simply the DA's overshooting premium: variants with all premiums set to zero were no better on wards (about 84.5%). The prime suspect is the attenuation logged in L003 and never corrected. The fitted slopes `b` are biased towards zero by sampling noise in the 2019 shares, which compresses spatial contrasts (which neighbourhoods lean to which party) towards the council average, and ward contests turn on exactly those contrasts. The national-vote rule uses the pattern unsquashed (effectively b = 1), which fits it winning on wards while losing on seats; and the zero-premium variants still used the squashed slopes. Secondary suspects: the ward/PR ticket-splitting ratio carried from the previous local election, and individual premiums that overshoot.

*Correction.* A share p among n votes has log-scale sampling variance of about (1 - p)/(np). Each party's reliability (lambda, the fraction of its observed spatial variance that is real) gives the classical errors-in-variables correction b / lambda, with the intercept re-set to keep the weighted means. On demo data with known truth, the mean slope error halves (0.057 -> 0.024), though DELTA FRONT recovers only from 0.77 to 0.81 of a true 0.90: the correction handles sampling noise, not every source of attenuation. With default settings the model is unchanged, as tested.

*Pre-registered experiment, fixed before any result.* 12 variants on the 2021 backtest: slopes {fitted, corrected, fixed at 1} x ticket-splitting ratio {on, off} x premiums {as fitted, halved}, at the spreads and newcomer setting in config.yml. **Rule:** choose the lowest mean ward Brier score (a proper score using the probabilities, not just the most likely winner) among variants whose mean seat CRPS is at most 0.005 worse than the current model's and whose established-party 90% coverage is in [0.80, 0.97]. Adopt it only if its ward Brier score beats the current model's. If no variant qualifies, including the current one, nothing changes. Ward accuracy, log score, seat error and council control are reported, not optimised. A per-ward table records, for every 2021 ward, the actual winner, the model's pick and the simple rules' picks, so the result can be explained whichever way it goes. The same run also carries the newcomer confirmation committed in L038.

**L038. Corrected backtest: decisions under the re-registered rules.** The corrected commitment was frozen first and verified before any result was read: tag `prereg-2026-r1` (commit `fafe787`), Release published 2026-09-25 12:19:30 UTC, SCORING.md identical to the corrected text, tests passing.

*Grid (all 24 settings on the same 390 cases).* The rule chooses **fitted premiums, province spread 0.35, council spread 0.15** (seat CRPS 0.4170; established-party coverage 85.6% at 90% and 50.3% at 50%; tails 13.9% low and 8.7% high, so actual seats fall below the forecast a little too often, consistent with the DA over-prediction). config.yml follows. The earlier choice, 0.55/0.45, ranks 12th of the 12 fitted settings (0.4344); its earlier win was the dilution artefact of E1. Keeping the learned premiums still wins by a wide margin (about 0.42 against 0.63-0.71), so that conclusion stands. The top settings are close (0.4170, 0.4185, 0.4194), but the differences are now exact and paired. Reported, not optimised: the chosen setting's council-control Brier score (0.497) is worse than the widest settings' (about 0.44).

*Newcomer module (run at the spreads then in config, 0.55/0.45).* Seat CRPS 0.4331 -> 0.4278, coverage 92.4%: **adopted** under the rule. The evidence on seats is weak: the paired difference is -0.005, with a 90% council-bootstrap interval of -0.032 to +0.023, which includes zero, and seat error rises slightly (0.504 -> 0.524). The clear gain is council control on the same 25 councils (48% -> 76%; Brier 0.447 -> 0.386). Ward winners are unchanged. Past newcomers' shares are almost uncorrelated across councils (rho = 0.07). **Committed before the next run:** the backtest reruns at the new spreads; if seat CRPS is then higher with newcomers than without, the module is switched off and logged.

*Versus the simple rules, corrected (0.55/0.45, common cases).* Seat error: model 0.50, national vote as-is 0.65, repeat previous local 0.77, so the model is better on seats, by less than the withdrawn figures claimed. Council control without newcomers: model 48%, national as-is 56%, previous local 48%. Ward winners: model 85.5%, national as-is 90.9%, previous local 87.2% (O7 stands). These are replaced by the rerun at the chosen settings.

**L037. Grid rescored; a flaw in the scoring design found; newcomer module not adopted on the flawed rule; corrections committed before rerunning.**

*Grid, rescored exactly as committed in L036.* The choice is unchanged: fitted premiums, spreads 0.55 and 0.45, seat CRPS 0.7148, exact 90% coverage 90.3%. Council control is noisy with 25 councils: settings within 0.003 CRPS of the chosen one hit 48-64%, so the 64% quoted for the chosen setting is partly luck.

*Newcomer test, as pre-registered in L035.* With newcomers: seat CRPS 0.7188 -> 0.5297, coverage 86.6%, so the literal rule says adopt. But the two runs were scored on different numbers of cases (235 against 315).

*The flaw.* A case was every party that won seats or had at least a 5% chance of one **under the forecast being scored**. A forecast that spreads probability more widely gets more cases, most of them easy (small chance, no seats), so its average improves without it being better. The same flaw affects the grid (cases rose from 215 at the narrowest spreads to 237 at the chosen ones) and the comparison with the simple rules (means over different denominators). On totals, the chosen grid setting has the *highest* total CRPS of the fitted settings (169.4, against 162.6 for province 0.35 / council 0.15), and the newcomer gain shrinks to about 1% (total 168.9 -> 166.8), while total absolute seat error rises (196.5 -> 204.5). Totals are not a clean comparison either, because the sets still differ, but they show the averages were misleading. Comparisons that were always on a common set are unaffected: council control with newcomers 48% -> 76%, Brier score 0.447 -> 0.386, and ward winners unchanged. **The pre-registered metric was badly specified; the error was mine, at design time.** The module is not adopted on it.

*Corrections, made before any corrected result exists.* (1) Every forecast and simple rule is scored on one case set: every party on a council's ballot, plus any seat winner, with a party a forecast does not include counting as zero seats. (2) Coverage is measured on a model-independent set fixed in advance: parties with 2%+ in that council at the previous local or latest national election. (3) The newcomer comparison also reports the paired CRPS difference, with a 90% interval from a bootstrap over councils. (4) SCORING.md is corrected (ERRATA E1) and frozen as `prereg-2026-r1` before the corrected backtest is run; the correction removes a bias that favoured the model. **Re-registered decisions, same rules on the corrected metric:** the grid chooses the lowest mean seat CRPS over the common cases among settings with established-party 90% coverage in [0.80, 0.97], and config.yml follows the choice, even if it changes. The newcomer module is adopted if its mean seat CRPS over the common cases is lower and its coverage is in the same band. The paired difference and its interval are reported whichever way it goes. The backtest's comparison with the simple rules, on the site and in SCORING.md, is withdrawn until then.

**L036. Error found while testing L035: the backtest scores contained avoidable randomness, and one comparison could depend on how a function call was written.** In the demo, "with newcomers" and "without newcomers" gave different seat CRPS (0.5108 against 0.5166) with zero newcomers. Cause: R evaluates function arguments lazily, so in `score_chain(simulate_chain(...))` the scorer set its seed *before* the simulation ran, and the simulation reseeds the generator itself. The score depended on random draws in two places: CRPS was estimated from a random subsample of 600 draws, and PIT coverage used one random number per case. Scoring noise was about 1%. The pre-registered grid (L027-L028) was chosen on CRPS differences of 0.001-0.004 among its top settings, which is within this noise. The surface had been flagged as flat, but not for this reason. Fixes: CRPS for whole-number seats is now computed exactly, as a sum over integers (identical to the old estimate on all draws, and deterministic). Coverage is the exact expected value of the randomised PIT (a closed form for discrete outcomes). The scorer evaluates the simulation before seeding. Newcomers' own random variation comes from a separate stream, so established parties see identical random numbers with and without them, making the newcomer test a paired comparison. The grid settings were already paired (fixed per-council seeds scale the same random numbers), so their ranking noise came only from scoring. **Committed in advance:** the next run rescores the 24 grid settings exactly. If the unchanged L027 rule (lowest CRPS among settings with 90% coverage in [0.80, 0.97]) then picks a different setting, config.yml is changed to it and logged, before the newcomer result is read.

**L035. Newcomer module built; pre-registered test written before any result.** *The problem.* In the blind 2021 backtest, parties the model could not see won 46 Western Cape seats. The 2026 candidate lists name every newcomer: 125 party-council cases, 56 parties, at least one in each of the 25 councils. That is over a quarter of the 480 party-council ballot entries. 108 of the 125 cases are parties with no Western Cape history at all. Most (90) stand in every ward, so ward coverage separates them less than expected; breadth separates them more (39 parties stand in one council, 4 in 11 or more: Afrika Mayibuye Movement 23, Cape Independence Party 22, SACP 21, Organic Humanity Movement 11). The **South African Communist Party**, standing on its own for the first time after contesting historically in alliance with the ANC, is unlike any past Western Cape newcomer; the class prior may describe it poorly (A17).

*The module.* Past newcomers are parties that stood in a council in 2016 or 2021 with no votes there in the previous local or latest national election (matched by conservative name key, so renamed parties are not counted). They are grouped by breadth (1 / 2-10 / 11+ councils) x coverage (all wards / partial), falling back to breadth alone, then to all newcomers, below 8 examples. Each simulated election draws each 2026 newcomer's council share from its class's historical distribution. A party's draws are correlated across councils, with the correlation estimated from past newcomers standing in several councils (default 0.5 with fewer than 10 pairs). The share is inserted after the established parties' shocks, varied across wards and voting districts, and taken proportionally from everyone else (A16). A party-specific prior can replace the class prior only through a sourced row in `data-raw/manual/entrant_priors.csv`.

*An error caught before any use.* The first version inserted newcomers before the shocks. The established parties' province and council shocks are symmetric on the log scale, but their sum is not, so newcomers were diluted by about 14% (median simulated share 6.8% against 7.9% drawn). Inserting after the shocks reproduces each draw exactly (to 10^-17). With no newcomers, the simulation is identical to the previous code in seats, ward winners and shares.

*Pre-registered test.* The 2021 backtest is run with and without the module at the chosen settings (fitted premiums; spreads 0.55 and 0.45, not re-tuned). The newcomer prior comes from **2016's newcomers only**, because using 2021's would be circular; this needs the 2011 local results (IEC id 197). **Rule:** adopt the module if seat CRPS is lower with it AND PIT coverage at 90% (modelled parties) lies in [0.80, 0.97]. If adopted, the 2026 forecast uses a prior pooled from 2016 and 2021 newcomers. The module is off (`model.entrants: false`) until then.

**L034. Scoring commitment re-issued and verified.** The tag `prereg-2026` is on commit `d98bd0a` in the re-created repository `csteyn/lge2026` (new repository ID; nothing from the deleted one survives). Its GitHub Release was published at **2026-09-25 10:01:48 UTC**, a server-side time that cannot be backdated. The tag was checked directly, not assumed. Its SCORING.md is byte-identical to the final version (so opening the file in Word did not alter it). It contains no forecast outputs, raw data or `targets` store. Its `.gitignore` excludes outputs and Office lock files. Its log ends with L033. The test suite passed on GitHub for the tagged commit, which also confirms the CI fixes L030-L032 in a clean environment. One check needed care: a web fetch of the Release page returned a cached copy of the deleted Release, identifiable by its old repository ID and request ID, so the live page was read directly.

**L033. The first pre-registration was withdrawn and re-issued, because it froze the wrong file and exposed a forecast.** A check of the tag `prereg-2026`, first published as a GitHub Release on 25 September at 08:56, found two errors. (1) It froze the pre-backtest draft of SCORING.md, not the final version (L029), because the files meant to be copied before the first commit (`.gitignore`, `SCORING.md`, `README.md`) were not. The log inside that tag already described the final version, so the frozen record contradicted itself. (2) Because the old `.gitignore` did not exclude `outputs/`, 65 output files were public, including a live Western Cape forecast run (25 September, 00:52 UTC). That forecast had not been reviewed for publication (L029). The repository was about three hours old, with no stars or forks, and the Release had not been cited anywhere. It was deleted, together with the tag and the exposed files, and re-created from a clean history with the correct files. The tag and Release were then re-issued: the Release on GitHub records the new date. Nothing in SCORING.md was changed except its opening sentence, which now says the commitment was fixed before any forecast was *deliberately* published and points here, because the earlier wording ("before any live forecast was published") was no longer strictly true. Lesson: before any first commit to a public repository, check the list of files being committed, not just their number.

**L032. Second GitHub run: binaries arrived, but package versions did not match.** The binary override from L031 worked. `renv.lock` recorded the versions on the Windows machine (for example `Rcpp` 1.1.1-1 and `S7` 0.2.1-1), while the Linux repository's "latest" set had already moved on (1.1.2 and 0.2.2). The exact versions were not available there: `S7` failed to download, `Rcpp` never arrived, and `units`, which needs it, failed its load test. Patching single packages would break again whenever CRAN moves. The fix is for the project, on Windows and on GitHub, to use one dated CRAN snapshot from Posit's package manager (24 September 2026), which serves identical versions as Windows and Linux binaries. This also freezes package versions for the rest of the campaign, matching the frozen scoring code. The run-time system libraries that renv reported as missing (GLPK for `igraph`, Abseil for `s2`, and others) are now installed by the workflow.

**L031. First run on GitHub: the tests could not start, because installing `units` (needed by `sf`) failed.** `renv.lock` was written on Windows and names plain CRAN, which offers only source code on Linux, so every package was compiled on GitHub's machine. `units` compiled but failed its load test. Fix: on GitHub, `renv` now installs Linux binaries from Posit's package manager, built for the runner's Ubuntu release, with the system libraries still installed for run time. Two notices on the same run were also acted on. `ubuntu-latest` switches to Ubuntu 26 on 19 October 2026, two weeks before the election, which would have silently changed the test environment mid-campaign, so the runner is now pinned to Ubuntu 24.04. And `actions/checkout` moved from v4 to v5, which runs on the newer Node.js. Not verifiable from our own sandbox (it cannot reach the package manager): the next GitHub run is the test.

**L030. `renv` left `testthat` out of the project library.** After `renv::init()`, running the tests failed with "there is no package called 'testthat'". `renv` records a project's packages by scanning its code for `library()` and `package::` calls. The tests call `test_that()` without naming the package, and the command that runs them lives in the CI workflow, which `renv` does not scan. A check of everything the scan detects found `testthat` was the only package missed. Fix: the test setup file now loads it explicitly, so every future snapshot records it. It would also have failed the first CI run on GitHub.

**L029. Scoring commitment finalised; version control and publication policy.** SCORING.md was rewritten before tagging, to name exactly what will be scored and how. The scope is the 25 Western Cape local and metro councils. The metrics are PIT coverage at 50% and 90%, seat CRPS, the median's seat error, council-control Brier score and hit rate, and ward-winner log score, Brier score and hit rate. Seats won by parties the model did not include count as a forecast of zero. The comparison is against the two simple rules. The truth is the IEC's final declared results. The code is the backtest scorer (`score_chain()`, `naive_rule()`) applied unchanged, and the 2021 backtest figures are included for reference, including the ward-winner loss (O7). It is frozen by the tag `prereg-2026`. A git tag's date is set locally and proves nothing on its own, so a GitHub Release made from the tag provides a server-side timestamp that cannot be backdated. Publication policy: the repository is public (method, log, assumptions, checks code and scoring commitment), but forecast outputs (`outputs/public/`, `outputs/archive/`) are kept out of Git, and the site workflow runs only by hand, until publication is decided. CI installs the system libraries that `sf` and `pdftools` need and takes its R version from `renv.lock`; neither workflow has yet run on GitHub.

**L028. The pre-registered experiment chose; my hypothesis lost.** Among the 24 settings fixed in L027, the rule chose **learned premiums kept, province spread 0.55, council spread 0.45** (seat CRPS 0.715; 90% coverage 91.3%; tails 8.2% low, 6.7% high). These are now the forecast settings. I had expected dropping the learned premiums to win, following the DA's 53-seat overshoot and the Johannesburg model's finding. It lost in all 12 comparisons: seat CRPS 1.02-1.25 against 0.72-0.78, seat error about 1.45 against 0.85. The premiums carry real information on balance (for example, that national parties generally lose ground to small and local parties in local elections); an individual party's premium can still overshoot, which the wider province spread now allows for. Had the choice been made after seeing results, the prior belief might have prevailed; pre-registering prevented that. Caveats: (1) the chosen setting is at the edge of the grid for both spreads, but the surface is flat there (the five best settings lie within 0.004 CRPS, probably within simulation noise at 400 draws; along the province axis at council 0.45 the gain shrinks with each step: 0.730, 0.724, 0.716, 0.715), so widening further would gain little and push coverage past 90%, and a province spread of 0.35 is essentially as good; (2) this is one election, and an unusual one (Covid, low turnout, a sharp DA decline); (3) the spreads measure blind uncertainty, with no polling. Against the simple rules, the chosen setting now wins on seats (error 0.83 against 1.46 and 1.70) and on council control (64% against 56% and 48%). **It still loses on ward winners** (85.5% against 90.9% for the 2019 national vote as it stands, roughly 20 of 406 wards); the likely cause is the same party-level premium bias. That is recorded as open item O7 and stated on the site. A fix (for example, partial shrinkage of premiums) would need its own pre-registered test.

**L027. First backtest results (2021 predicted blind), and a pre-registered experiment.**

*Results, config settings (province 0.10, council 0.15).* Seat error per party per council: model 0.93, against 1.70 for repeating 2016 and 1.46 for the 2019 national vote as it stands, so the model is clearly better on seats. Council control correct: model 48%, repeat-2016 48%, national-as-is 56%. Ward winners correct: model 85.5%, repeat-2016 87.2%, national-as-is 90.9%. **On wards and council control the model is worse than simply using the 2019 national vote.** Uncertainty too narrow: for parties the model could see, 90% intervals caught the actual seats 83% of the time. Spreads estimated from 2021 (province 0.57, council 0.47) overshoot to 97%. The largest single error: the translation learned from 2014 -> 2016 gave the DA a local premium of a = +0.98 (its strong 2016 local result). Carried into 2021, when the DA declined, it over-predicted the DA by 53 seats across the province and turned hung councils into predicted DA majorities (Saldanha Bay's actual outcome got 2%). The EFF's learned premium (+0.55) did the same on a smaller scale (+16 seats). The spatial slopes `b` were mostly 0.8-1.0. This independently reproduces the Johannesburg model's finding that spatial structure transfers between cycles and the level ("political weather") does not. It bears directly on 2026, which carries the premiums learned from 2019 -> 2021. Parties with no history won 46 seats: PA 19 (no Western Cape history in 2016 or 2019), Cape Coloured Congress 8, and about 19 for local civic parties in roughly 12 of the 25 councils. For 2026 the PA is visible; new civic parties will recur, and the 2026 candidate lists name them, which the model does not yet use.

*Measurement fix.* Coverage from quantile intervals over-states calibration for small whole-number seat counts (the "50%" interval of a party with 0-2 seats covers most outcomes). Coverage is now also measured from the randomised PIT, which is exact under calibration.

*Pre-registered before any result is seen.* 24 settings: learned premiums {fitted, zero} x province spread {0.10, 0.20, 0.35, 0.55} x council spread {0.15, 0.30, 0.45}. **Rule:** choose the lowest mean seat CRPS over all party x council cases, among settings whose PIT coverage at 90% (modelled parties) lies in [0.80, 0.97]. Everything else (ward log score, council-control Brier, the simple rules) is reported, not optimised. Choosing among 24 settings on one election risks fitting noise, so the chosen setting must also beat the simple rules on seats, and any decision to drop the learned premiums rests on two independent pieces of evidence (this backtest and the Johannesburg model's two-cycle validation), not on this ranking alone. The chosen setting will be carried into the 2026 forecast, with its effect on the forecast logged.

**L026. Backtest built: predict 2021 blind, score it, estimate A02.** The model is run as if it were 2021, with inputs shifted back one cycle: the translation is learned from 2014 national -> 2016 local, the latest national vote is 2019, the previous local election is 2016, and who stood comes from the 2021 results (the equivalent of the 2026 candidate lists). No 2021 total is ever seen, following the Johannesburg model's finding that a backtest given the real citywide result looks five times better than a blind one. The prediction is scored against the IEC's official 2021 results: interval coverage, a PIT histogram, CRPS, council-control and ward-winner probabilities. It is compared with the two simple rules in SCORING.md. The A02 spreads are then estimated directly: each party's council-level surprise is split into a province-wide part (the political weather) and a council-specific part, and the backtest is rerun with the estimated values to check that calibration improves. Data: the 2016 local results download automatically; the 2014 national results are a browser download from the IEC's main site (the results portal does not carry them) in a different layout, with their own reader. Backtest downloads never stop the forecast. Caveats recorded in advance: one election gives only a handful of parties for the province-wide spread, so that estimate is rough; parties with no history (for example the Cape Coloured Congress, 7 Cape Town seats in 2021) are invisible to the model, and the backtest counts the seats this costs. Built and tested on demo data, where the backtest is in-sample and its scores mean nothing; the real run is pending the 2014 download. Also fixed: check C06 showed only the first party's premium; it now lists each party with its own evidence.

**L025. The last two name questions ruled by local knowledge (C.E. Steyn).** `BUILD ONE SOUTH AFRICA` on the 2026 lists is the same party as the 2024 `BUILD ONE SOUTH AFRICA WITH MMUSI MAIMANE` (BOSA), confirming the rename entered in L024. `UNITED PROGRESSIVE PARTY SOUTH AFRICA` (2024) and `UNITED SOUTH AFRICA` (2026) are different parties, now recorded as distinct. Check C17 should have no open items.

**L024. Three more party-name mismatches, and a permanent detector.** Building the by-election estimate showed that the 2024 results name `UMKHONTO WESIZWE` while the 2026 candidate lists say `UMKHONTO WESIZWE PARTY`. The MK is on 23 Western Cape councils' PR lists, and the model had been treating it as not standing in all of them. It was below 2% everywhere in the province, so C15 never saw it. An audit of every party marked as not standing, against every 2026 list name in the same council, found two more: `THE ORGANIC HUMANITY MOVEMENT`, and `BUILD ONE SOUTH AFRICA WITH MMUSI MAIMANE` (the 2024 ballot carried the leader's name). Mismatched party names are now the most frequent error class in this project (L018, L020, L024). Fixes: names that are identical apart from a leading "The", a trailing "Party" and punctuation are matched automatically; the BOSA rename is in the register, flagged for confirmation; and new check C17 lists every remaining near-match between a party marked not standing and a name on the same council's list, until a human rules it the same party (`renamed`) or a different one (`distinct`) in the register. Four near-matches were ruled distinct; one (`UNITED PROGRESSIVE PARTY SOUTH AFRICA` / `UNITED SOUTH AFRICA`) was left open for local knowledge, and ruled distinct in L025.

**L023. The Patriotic Alliance's local premium, estimated from by-elections (O5 step 2).** For each voting district of a Western Cape by-election ward in which the PA stood, its vote relative to the parties with a fitted translation was compared with the prediction from that district's 2024 vote; residuals were vote-weighted within wards and averaged across wards. From 14 wards since the 2024 national election, the premium is +0.20 ± 0.19 (log-ratio scale): the PA does somewhat better in local elections than its 2024 national vote implies. Combined by precision with the prior, it is +0.12 ± 0.17, drawn afresh in every simulation. A placebo, the same estimate for fitted parties whose premium is already in the model, came out consistent with zero (ANC −0.05 ± 0.19, DA −0.23 ± 0.23, EFF +0.28 ± 0.40), so no systematic-error term was needed. The window was chosen on that placebo, not on the PA result. Including pre-2024 by-elections, the placebo parties drift (systematic error 0.35), because those by-elections predate the 2024 baseline they are compared with, and the PA estimate falls to −0.18. That sensitivity is recorded here and in A14. **Correction to L022:** the step 1 prior centres on the fitted parties' average premium, −0.32. On its own it would have lowered the PA's expected local share by about a quarter; the evidence points the other way. It was never used in a live run. A prior built from typical parties describes an atypical, locally organised party poorly, which is why step 2 was needed. The National Coloured Congress, the People's Movement for Change and the ATM have fewer than 3 by-election wards in the modelled councils and keep the prior. By-election party names are abbreviations; they are mapped to results names through `byelection_abbreviations.csv`, which lists only mappings we are sure of.

**L022. Parties with no fitted translation no longer get a certain one (step 1 of 2 on O5).** The Patriotic Alliance was not on the 2019 Western Cape provincial ballot (confirmed: no PA rows in the 2019 file), so the 2019 -> 2021 fit has nothing to learn its local premium `a` from. Until now such parties got `a = 0` with no uncertainty, which is an assumption that local and national support are identical, with false confidence. The PA won 28 seats in 21 Western Cape councils in 2021, and in 14 of them no party had a majority; its 2021 share was 22% in Beaufort West, 14% in Prince Albert and Laingsburg, and 12.5% in Matzikama. Now `a` is a distribution. The prior is the mean and spread of `a` among the fitted parties, drawn afresh in each simulation and shared across councils within a draw (a party's local premium is a property of the party). This widens intervals where such parties are large; it does not correct any bias. Step 2 will estimate the premium from the PA's by-election results since 2021 and combine that with this prior by precision. That step will be built against the real by-election data, not a guessed layout (A13).

**L021. Disputes resolved, and a modelling error found through them: votes of parties no longer standing were being given to small parties.** On re-checking, local knowledge agrees with the certified list: none of the five flagged parties is standing in 2026 under any name, so the two disputed entries (L020) are now confirmed absences and C15 has no open items. Working out what that means for the forecast exposed an error. When the candidate data showed a party was not standing, the code dropped it from the party table, so its past votes fell through to the catch-all OTHER, which is later split among the small parties when seats are allocated. GOOD's 8% in Beaufort West, or the Karoo Gemeenskap Party's 11% in Prince Albert, was being handed to minor parties. A test reproduced it: a non-standing party with 10% tripled OTHER from 5% to 15%. Fix: non-standing parties are kept and marked absent, and their votes are removed and the standing parties' shares renormalised within each voting district, both in the 2026 baseline and in the 2019 -> 2021 transfer fit and ward/PR ratios. That redistribution is itself an assumption, now A12.

**L020. Local knowledge checked against the certified candidate list: three confirmations, two conflicts.** Check C15 flagged five parties with 2%+ support in 2021 or 2024 that are not on a 2026 PR list. Local knowledge (C.E. Steyn) was compared with the IEC's certified Western Cape list of 16 September 2026, which is fully text-searchable (271 pages, every page carrying candidate rows) and whose parsed totals match the IEC's published figures exactly. Confirmed absent: the Karoo Ontwikkelings Party (Laingsburg) and the United Community Front (Prince Albert), which have dropped out, and GOOD in Beaufort West: GOOD stands in 19 Western Cape councils and districts, but not Beaufort West or the Central Karoo district. In conflict: local knowledge holds that the Oudtshoorn Gemeenskap Inisiatief returns as "Community Initiative / Gemeenskap Inisiatief" with candidates in the Garden Route district, and that the Karoo Gemeenskap Party (11% in Prince Albert in 2021) is definitely standing. The certified list contains neither name anywhere, including the district list. Both are recorded as disputed in a new register, `data-raw/manual/party_status_2026.csv`, which quotes both sources, is published on the site's checks page, and is read by C15 (confirmed absences stop being flagged; disputed ones stay flagged) and by candidate-name matching (confirmed renames map a 2026 name onto its old results name). Until a dispute is settled the model follows the certified list (A11).

**L019. Error, found by check C14: we applied the excessive-seat rule on equality; the IEC does not.** Validated against the IEC's published 2021 calculations for all 25 Western Cape councils, 19 matched and 6 failed. In five (Matzikama, Cederberg, Kannaland, Bitou, Prince Albert) the seats were identical, but we flagged a party as "excessive" because its ward wins equalled its entitlement, and the IEC did not. In Beaufort West the seats differed: the ANC won 4 wards with an entitlement of exactly 4; treating that as excessive fixed the ANC at 4, recalculated the other 9 seats without it, and moved one seat from the DA to GOOD (ours DA 3, GOOD 2; IEC DA 4, GOOD 1). In a 13-seat council one seat can decide control. With the trigger changed to strictly greater, all 25 councils are reproduced exactly, including the three genuine excessive-seat cases (Mossel Bay, Oudtshoorn, Laingsburg), whose parties match the IEC's own markings. We had adopted "equal to or greater" from the Johannesburg model's documentation without testing it on a council where equality occurs; its own Johannesburg validation would not have exposed the difference. This also closes assumption A03, since a party with no wards can never strictly exceed its entitlement. Also fixed: the workbook reader took a party name as the council code in three councils; it now reads the "Municipality:" row only. All 25 councils are now regression tests, and the test that had asserted the old rule was reversed.

**L018. Candidate lists: the real layout, and three problems it exposed.** The IEC's Western Cape list (16 September, 271 pages) is one table with no section headings; the "Ward \ List Order" column holds either a PR list position or an 8-digit ward ID, and that is what separates ward candidates from PR lists. Our parser allowed at most 4 digits there, so every ward row failed (O4 resolved). Rewritten around this, it parses 4 502 PR rows (IEC: 4 502) and 9 846 ward rows (IEC: 9 847) covering all 416 wards in 30 councils. The missing ward candidate is a Drakenstein row printed with a malformed ID (`102010****0 *`); it now parses, and its impossible date yields no birth year rather than a false one (1910). Our quarantine count had also missed that row, because it only counted rows with a well-formed ID; it now counts every table row that fails to parse. Most seriously, the PDF prints some party names bilingually (`VRYHEIDSFRONT PLUS | FREEDOM FRONT PLUS`), unlike the results files; matched naively, the FF+ would have been treated as absent from every 2026 ballot. Names are now harmonised against the results files. New check C15 lists every party with 2%+ in 2021 or 2024 that is not on a council's 2026 PR list, for a human to confirm; C16 compares parsed candidate counts with the IEC's published totals.

**L017. The seat allocator reproduces published councils exactly.** Fed the IEC's own vote totals and ward wins from its 2021 Seat Calculation Detail workbooks, our Schedule 1 implementation reproduces Cape Town exactly (50 parties, quota 7 849, all 231 seats, DA 135) and Laingsburg exactly, including the excessive-seat case: the DA's 3 wards exceeded its entitlement, and the IEC's own sheet prints the recalculation formula `Q = (A-B)/(C-(D+E+F)) + 1` that we implemented. Both are now regression tests, and every live run validates all Western Cape councils (check C14). The IEC workbooks put party names in different columns in different councils, so the reader locates columns by their header text.

**First complete live run (2026-09-24).** All 25 Western Cape councils simulated from real IEC and MDB data. Geography reproduces the IEC's 416 wards and 1 623 voting districts exactly (C11). `REGPOP` turned out to be empty for every Western Cape district, so registration now comes from the IEC's 2024 counts for 1 559 districts and ward medians for 64 new ones (C12, L016).

**L016. Registration missing for every Cape Town voting district: fallback to the IEC's own 2024 counts.** The input validation added in L015 stopped the Cape Town simulation with a named cause: 829 of 829 VDs had no registration after imputation. So the `NA` in L015 almost certainly came from registration, as suspected. With every VD in the council blank, a median fallback has nothing to work with. Not yet known: whether `REGPOP` is empty in the Demarcation Board layer for Cape Town (or everywhere), or stored in a form we read as missing. Changes: text values such as `1 234` are now parsed; registration is taken from the MDB layer where present, otherwise from the IEC's 2024 count for the same VD number (in the national results file), and only then from ward or municipality medians. Check C12 reports how many VDs came from each source, and fails if medians fill more than 5%. Caveat recorded: where a VD was split in 2026, the part that kept the old number inherits the whole 2024 count, and the new part gets a median. This overstates registration in split areas until a better source is found (A06).

**L015. Error, found on the first live simulation: "missing value where TRUE/FALSE needed" inside a council simulation.** The memory fix (L014) held: the transfer fit, the baseline and 25 simulation branches ran on real data for the first time. One branch then failed deep in the seat arithmetic. The exact cause is not confirmed, but the code had two routes by which a single missing value could turn a whole council's votes into `NA`. First, `weighted.mean(na.rm = TRUE)` drops missing values but not missing weights, so one voting district with a blank registration count made its ward and municipality averages `NA`. Second, a ward or VD spread that the transfer fit could not estimate would have been passed on as `NA`. Fixes: registration gaps are imputed from ward, then municipality, medians and counted (check C12); turnout imputation is counted (C13); averages ignore incomplete pairs; any share still missing is filled explicitly. The simulation now checks its inputs first and stops with the named cause, and the seat allocator refuses non-finite votes by party name. The lesson: validate at the boundary between data and arithmetic, where the cause is still visible.

**L014. The pipeline's worker process was killed after the national files were parsed (probably out of memory, not yet confirmed).** Party grouping now works on real data (the L013 fix held), but the run died starting the transfer fit, with no R error. By default `targets` keeps every finished result in memory, and `inputs` held all nine provinces' national results (about 2.4 million rows from 170 MB of text) although only the Western Cape is modelled. Changes: results are dropped from memory once used (`memory = "transient"`); only modelled provinces' lines are parsed, with a loud fallback to all lines if the province labels don't match; memory is freed after each file. Raw national files remain on disk, so collection is still national. If the crash recurs, the next step is to run the transfer fit interactively to see whether the cause is something else.

**L013. Design error, found on the first full live run: a failed candidate-list parse removed every party from every council.** The Western Cape candidate PDF appears to have downloaded, but our parser (never tested on the real layout, O4) extracted no usable rows. The code treated the resulting empty table as "no party is on the 2026 ballot" and filtered everything out; the next step then failed with an unhelpful column error. Two fixes. Candidate data is used only if it parses into rows whose ward and PR sections can be told apart; otherwise it is treated as unavailable, the reason is shown in check C10, and assumption A07 applies. And candidate filtering applies only in municipalities the data covers. Empty inputs to the party-grouping and transfer steps now stop with a message that names the cause. Also confirmed on this run: the by-election download and both national-election files were fetched and parsed.

**L012. Error, found on the first full live run: the by-election download stopped the pipeline.** One event's data file came back empty, and httr2 throws an error when asked to read an empty response, before our own emptiness check could run. The target was meant to be unable to stop the run (`error = "continue"`, L009), but the copy of `_targets.R` in use predated that change: a symptom of updating the project file by file. Fix at the source: `fetch_byelections()` is now a total function that never throws; failed or empty requests are written to the fetch log instead. Tested with the network entirely unreachable. Also confirmed from the files: the 2019 national export is `827.csv` (the 2019 provincial-ballot id) and the 2024 export `Provincial.csv`, so the years are the right way round; the parser handles 10^6 lines in about 8 s.

**L011. First live data verified; one wrong field-name guess fixed before it failed.** The 25 Western Cape 2021 results files parse cleanly: every file holds the council its name says, and the district ballot is labelled as assumed (`DC40`). Their totals, 406 wards and 1 577 voting districts in 2021, are consistent with the IEC's 2026 figures of 416 and 1 623 after ward redrawing. Printing the live Demarcation Board field names showed that `VotingDistricts2026_Final` has no `Split_VD` field (mentioned in the Johannesburg notes), which our code required. It also has a direct `WardId`, now used instead of reconstructing wards from `WardNo`. A test fixture with the exact live field names guards against future schema changes. Pre-emptive change: national-election rows are now assigned to municipalities by voting-district number where possible, rather than trusting the file's municipality label.

**L010. Error, found on the first live run: the IEC download sweep stopped at its second file with "invalid multibyte string".** The first file (Cape Town's 2021 results CSV) downloaded, which is the first confirmation that the IEC report URLs work from this code. The second, a binary `.xls`, crashed the check that detects HTML error pages served as data, because it lowercased raw binary bytes as if they were text. The function was only ever tested on text files. Fix: inspect printable ASCII bytes only, and never let an inspection failure stop the sweep. Tests now cover `.xls`, `.pdf` and a BOM-prefixed HTML page.

**L009. Added check C11: the Western Cape geography must reproduce the IEC's published totals** of 416 ward seats and 1,623 voting districts (IEC briefings reported by IOL, 15 and 23 September 2026). This is the first independent test of the Demarcation Board layers the model is built on. Also: the by-election and Stats SA downloads, collected for v0.2 but unused, can no longer stop the pipeline if they fail.

**L008. Error, found on the first run outside the development sandbox: `tar_make()` failed at `site_pages` with "cannot open file for writing: site/m/DEM1.qmd".** Cause: `site/m/` was empty in the repository, Git does not store empty folders, so a fresh copy had no such folder, and `sync_site()` assumed it existed. It passed in development because the folder existed there. Fix: `sync_site()` creates the folder. Process fix: releases are now tested by running the pipeline from a fresh `git archive` of the repository, not from the working copy.

**L007. Error, fixed before publication: a party's chart colour could change between municipality pages.** Fallback colours were assigned per page from whichever parties appeared there. Fix: assign once over all parties in the outputs (`site/_common.R`). Found by inspecting the rendered demo charts; also fixed there: a clipped axis label and a meaningless alpha legend on the ward map.

**L006. Decision: the excessive-seat rule is applied only to parties that won at least one ward (A03).** A party with no wards and no entitlement satisfies "equal to or greater" trivially; removing it from the recomputation looks unintended. Not yet confirmed against the Schedule 1 text or an IEC calculation. Status: open.

**L005. Decision: OTHER never wins seats as a bloc.** Its votes are split into its member parties in fixed proportions before seats are allocated, and a ward "won by OTHER" goes to its largest member (A04). Consequence: seat counts for very small parties are rough.

**L004. Error, fixed before publication: baseline shares in imputed VDs summed to 0.97, not 1.** Cause: new VDs averaged *log* shares across their ward, which is a geometric mean, and by Jensen's inequality the result sums to less than one. The simulation renormalises, so no output was affected, but the baseline file was wrong. Fix: renormalise every VD in `build_baseline()`; a test now asserts sums of exactly one.

**L003. Error, fixed before publication: the transfer was being fitted for parties that did not exist in 2019.** Their smoothed near-zero shares were regressed on smoothed near-zero shares, producing a meaningless slope. Fix: a party must have real votes in both elections in a municipality to inform the fit. Also found, **not fixed**: slopes are attenuated by noise in the predictor (0.78 fitted against 0.90 true on synthetic data). Logged as a known bias in METHODOLOGY; check D01 tracks it in demo runs.

**L002. Error, fixed before publication: `allocate_seats()` crashed on every call.** Inside `tibble()`, the column `votes` shadowed the argument `votes`, so `names(votes)` returned nothing. Caught by the first test run. The simulation uses the separate `schedule1()` core and was never affected.

**L001. Project started.** Scope: collect nationally, model and publish the Western Cape first. Method follows the published Johannesburg model (voting-district level, both ballots, the statutory seat formula, full coalition enumeration), reimplemented in R. Endpoint knowledge inherited from its SOURCES.md is marked unverified until our own first successful fetch.
